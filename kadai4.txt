画像処理演習  第4回 

■テーマ1：テンプレートマッチング基本プログラムの実行
  テンプレートマッチング用のウインドウプログラム(theme4-1.c)をダウンロードして実行せよ．また，中身を十分理解せよ．
  サンプルプログラム(theme4-1.c) は別ファイルにて添付


■テーマ2：対象画像中からテンプレート画像と似ている領域を，ユークリッド距離から推定せよ．なお，マッチング時の領域の左上座標及び距離値も表示されるようにすること．
考え方

基本画像の(x,y)画素を始点とし，サイズがTI×TJの小領域画像とテンプレート画像との間の関係を計算する．
例えば，，，

void
e_distance()
{
    int si,sj,syori,n;
    float d,dmin;

    dmin=1000.0;
    n=0;
    syori=(Isize-TI-1)*(Jsize-TJ-1)/100;
    for(__________){      //基本画像内でサイズTI，TJとなる小領域を設定
        for(__________){
            d=0;
            for(__________){      //テンプレート画像との距離値の計算
                for(__________){
                    距離値の計算
                    ＊＊単に積和計算すると，オーバーフローの可能性あり．
                }
            }
            if(d<dmin){  //距離値が最低となる始点を記憶する．
                ________;
                si=__; sj=__;
            }
            if(n%syori==0)printf("%d％終了\n",n/syori);
            n++;
        }
    }
    printf(       );  //計算結果(領域の左上座標と距離値)の表示
                       //si,sjを始点とする．
    set_quad(si,sj,TI,TJ);
}


■テーマ3：対象画像中からテンプレート画像と似ている領域を，相互相関関数を用いて推定せよ．なお，マッチング時の領域の左上座標及び相関係数も表示されるようにすること．
考え方

ユークリッド距離による判定を相互相関係数の式に置き換えて，類似度が高い部分を検出結果と判定するように改良する．


□ 提出課題
  1．画像lena_temp1～3は，画像lenaのどの位置に相当するか座標を答えよ．また，このときのユークリッド距離，相関係数を求めよ．
  2．画像baboon_temp1,2は，画像baboonのどの位置に相当するか述べよ．このときのユークリッド距離，相関係数を求めよ．
  3. それぞれの結果について自分なりに考察せよ．
